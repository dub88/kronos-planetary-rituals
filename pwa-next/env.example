# Copy to .env.local (not committed) and edit.

# Default coordinates used by the UI when geolocation is unavailable.
NEXT_PUBLIC_DEFAULT_LAT=40.7608
NEXT_PUBLIC_DEFAULT_LON=-111.8910

# Default timezone used by the UI if Intl API is unavailable.
NEXT_PUBLIC_DEFAULT_TZ=UTC
